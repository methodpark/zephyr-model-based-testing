CONSTANT defaultInitValue = defaultInitValue
\* Add statements after this line.
SPECIFICATION Spec

\* The following properties should hold, otherwise something is wrong in the model
INVARIANT TypeInvariant
INVARIANT MutexInvariant

\* This invariant will cause the model checker to report counterexamples.
\* Remove or comment it to do model-checking using only above invariants.
INVARIANT TestInvariant

CONSTANT
  N_THREADS = 2
  N_MUTEXES = 1
  N_SEMAPHORES = 1
  N_PREEMPT_PRIOS = 2
  N_COOP_PRIOS = 0
  NULL = NULL
  K_FOREVER = K_FOREVER
  K_NO_WAIT = K_NO_WAIT
  IDLE_THREAD = IDLE_THREAD
  ThreadPrio <- ThreadPrioImpl
  ThreadPrioNextState <- ThreadPrioNextStateImpl


VIEW CustomView
CONSTRAINT CustomConstraint

\* Remove the ALIAS to disable counterexample reformatting
ALIAS Alias
