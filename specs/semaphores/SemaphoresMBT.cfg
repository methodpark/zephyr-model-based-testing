CONSTANT defaultInitValue = defaultInitValue
\* Add statements after this line.
SPECIFICATION Spec

\* The following properties should hold, otherwise something is wrong in the model
INVARIANT TypeInvariant
INVARIANT SemaphoreInvariant

\* This invariant and property will cause the model checker to report counterexamples.
\* Remove or comment these lines to do model-checking using only above invariants.
INVARIANT TestInvariant
PROPERTY TestProperty

CONSTANT
  N_THREADS = 2
  N_SEMAPHORES = 1
  N_PREEMPT_PRIOS = 2
  N_COOP_PRIOS = 1
  K_SEM_LIMIT_MAX = 2
  NULL = NULL
  K_FOREVER = K_FOREVER
  K_NO_WAIT = K_NO_WAIT
  IDLE_THREAD = IDLE_THREAD
  ThreadPrio <- ThreadPrioImpl

VIEW CustomView

\* Remove the ALIAS to disable counterexample reformatting
ALIAS Alias
